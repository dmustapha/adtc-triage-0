# Triage-01 canonical runtime requires no environment variables or credentials.
# Model identity, runtime flags, and clinical policy are committed configuration files.
# The Devpost team ID is passed explicitly to `npm run freeze-model -- <team-id>`.
# Release signing creates `.release-private-key.pem` locally; never place it in an env var or commit it.
