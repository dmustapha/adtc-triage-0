# Stress Test Report: Triage-0 ADTC

> Generated by hackathon-stress | 2026-08-24T20:45:00Z
> Confidence Score: 81/100, Solid

## Summary

| Metric | Value |
|---|---:|
| Planned Stress case groups | 86 |
| Passed without a fix | 74 |
| Failed, then fixed | 1 |
| Unresolved | 2 |
| Exact prerequisite skips | 9 |
| Repository regression suite | 286 total, 264 pass, 0 fail, 22 exact skips |
| Confidence | 81/100 |

The score covers the claim-limited local build only. It grants no credit for live QVAC, model-backed clinical behavior, reviewed clinical content, physical Ubuntu thermals, profiler evidence, signing, publication, or submission readiness.

## Coverage declaration

| Tier | Features | Coverage achieved | Skipped edge-case classes |
|---|---:|---|---|
| P0 | 12 | All features received local contract, static, HTTP, browser, or prerequisite-probe coverage | Real GGUF/QVAC runs, model memory/thermal soak, physical Ubuntu, final media/publication |
| P1 | 0 | Vacuous, the frozen PRD has no P1 implementation obligation | Not applicable |
| P2 | 0 | Vacuous, the frozen PRD has no P2 implementation obligation | Not applicable |

No P0 feature had zero coverage. Unavailable environment-dependent paths are exact skips, not inferred passes.

## Phase results

### Frontend journey

- The structured-observation flow enables guidance only after a supported age and all seven observations are recorded.
- A deterministic emergency completed in real Chrome without QVAC and emitted a terminal card.
- An XSS-shaped note stayed text and created zero script nodes.
- The setup banner truthfully reported zero guideline chunks and no resident-model proof.
- Unsupported speech and translation controls remained absent.
- Keyboard tab order reached the primary input, example, progressive disclosure, and log link. Enter opened the disclosure.

### End-to-end flows

| Flow | Duration | Events/final state | Result |
|---|---:|---|---|
| Deterministic emergency assessment | under 2 s | stage, citation, card, done; terminal emergency card | PASS |
| Supported all-absent MedPsy assessment | not run | canonical GGUF and ingested QVAC store absent | SKIPPED, prerequisite |
| Model-backed cancellation and retry | not run | canonical GGUF and ingested QVAC store absent | SKIPPED, prerequisite |

The deterministic result exposed the existing diagnosis-first classification and antibiotic/management boundary. It remains an unresolved claim blocker below.

### Visual and accessibility

- Real Chrome screenshots were captured at 320, 768, 1024, 1440, and 1920 CSS pixels.
- Every viewport had `scrollWidth === innerWidth`; no horizontal overflow was observed.
- The original compact hierarchy and progressive disclosure remained intact.
- Browser reachability changed exactly between `Browser online` and `Browser offline`.
- The pre-run panel showed no resident model, no on-device proof chip, and no completed-inference metric.
- Browser console result: 0 errors and 0 warnings.

### Backend API

- Valid `/health`, `/app`, `/perf-log`, and deterministic `/triage` requests returned expected local responses.
- Empty, whitespace, malformed, overlong, and oversized requests returned bounded 400 or 413 responses.
- Ten concurrent deterministic emergency requests all returned 200 with terminal `done` events.
- Traversal-shaped `/../../etc/passwd` returned 404 without file disclosure.
- Wrong methods return Express HTML 404 rather than explicit JSON 405. This remains ST-API-METHOD-01.

### Contract

Smart-contract stress was skipped because the frozen ADTC architecture contains no smart contract or on-chain state.

### State, network, timing, and recovery

- Malformed model/SSE frames, unexpected fields, unknown source identifiers, and corrupt store metadata are covered by fail-closed tests.
- The browser stores no case or assessment state by default; static assets require revalidation.
- The browser network ledger contained only `127.0.0.1` requests.
- The response-deadline test proves a timeout does not release the inference queue early.
- Production startup fails closed before listening when canonical GGUF bytes are absent.
- The owned listener accepted SIGINT, closed port 58507, and left no Stress listener, capture, or installer process.

## Fix applied

| # | Test ID | Files | Description | Verified |
|---|---|---|---|---|
| 1 | ST-008-CLAIM-01 | `public/app.html`, `tests/debug-ui-claims.test.ts`, `config/import-manifest.json` | Removed unconditional pre-run “Nothing leaves this computer” copy. The replacement waits for local egress-guard evidence. | Focused RED observed; 18 related tests green; provenance green; full suite 264/0/22; five widths green |

## Unresolved issues

| # | Test ID | Type | Description | Attempts | Impact |
|---|---|---|---|---:|---|
| 1 | ST-006-CLAIM-01 | UNRESOLVED-BUG | Deterministic emergency UI still renders diagnosis-first classification plus antibiotic/management-plan language beyond the controlling narrow PRD claim. | 0 | Clinical advancement remains HOLD; resolution requires a clinical/product decision. |
| 2 | ST-API-METHOD-01 | UNRESOLVED-BUG | Known paths called with the wrong method return Express HTML 404 rather than explicit JSON 405. | 0 | Low product impact, but API method semantics remain incomplete. |

`REPORT.md` remains stale and is already owner-routed to Deploy as DI-W1. Verify HOLD remains unchanged.

## Exact prerequisite skips

1. Clean public canonical-GGUF download against the real 1,282,439,360-byte asset.
2. Supported all-absent QVAC two-pass assessment.
3. Model-backed cancellation and clean retry.
4. Repeated four-thread cold/warm inference.
5. QVAC cold load, warm use, unload, and absence measurement.
6. Full model-backed no-egress run.
7. 8 GB process-tree memory constraint and leak soak.
8. Physical target-laptop thermal soak.
9. Final profiler/media/publication/submission path.

These skips reconcile with the repository suite's 22 finer-grained prerequisite skips.

## Critique checkpoint

- Approved elevations checked: 5
- Verified as locally represented: 2, the raw-evidence gate contract and exact same-byte runtime parity contract
- Implemented during Stress: 0
- Open: named review, physical 8 GB evidence, and final proof-led media

## Verification evidence

- `npm test`: 286 total, 264 pass, 0 fail, 22 skip.
- `npm run typecheck`: exit 0.
- `npm run verify-import-manifest`: 76 completed imports verified from `74424721bc75f564808eacce42d7f7f42676ae0f`.
- `git diff --check`: exit 0.
- Stress state and import-manifest JSON parse.
- No application GGUF, `.part`, or `.partial` file exists. The only located GGUF belongs to an installed dependency under `node_modules`.
- Secret scan found no embedded token or private-key material.
- Failed run `32742482642` and historical evidence were not changed.

## Process cleanup

The owned ephemeral server was stopped. Port 58507 is closed, and no Stress Playwright screenshot, Chromium installer, or ADTC server process remains.
