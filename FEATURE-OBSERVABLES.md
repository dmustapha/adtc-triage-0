# Triage-01 — Feature Observables

Every observable proves behavior with real artifacts. A sentinel failure blocks the feature; it never authorizes a mock, fabricated clinical output, or synthetic benchmark.

## Controlling structured-danger-v1 observables

This revision supersedes conflicting clean-build/direct-product-runtime rows below. Structured age owns the 2-through-59-month IMCI scope. Six structured keys can establish emergency before semantic routing or MedPsy; chest indrawing is not an emergency alone and follows its age-scoped non-emergency pneumonia branch. Missing values become `NOT_ASSESSED`, and internal `CONFLICT` fails closed. `card.red_flags` stays wire-compatible but is structured-only.

`medpsy-product-v2` proves the real QVAC SDK 0.13.3 local-RAG/two-pass/retry product path. `medpsy-raw-profiler-v2` proves the exact same GGUF under pinned official llama.cpp and cannot prove product safety. Historical run `32742482642` is immutable; identity placeholders, human review, physical evidence, signed decision, and Phase 2 remain blockers.

| Feature ID | P0 feature | Observable | Exact test command | Sentinel failure | Verified by |
|---|---|---|---|---|---|
| FTR-001 | Canonical model contract and anonymous provisioning | First download matches locked bytes/SHA/path; second run preserves the same inode content | `./download_model.sh && sha256sum -c evidence/model.sha256 && ./download_model.sh && sha256sum -c evidence/model.sha256` | Credential prompt, path drift, bytes/hash mismatch, or second-run replacement | `tests/downloader.test.ts`, Debug clean-clone |
| FTR-002 | Mandatory finalist model gate | Signed decision references real raw artifacts for every mandatory gate and exactly one winner | `npm run build-finalist-bundle && npm run finalist-gate && node --import tsx --test tests/finalist.test.ts` | Missing producer manifest/raw artifact, false gate, invalid signature, tie, or freeze before pass | Build model gate, Verify milestone |
| FTR-003 | Split runtimes, one exact GGUF | QVAC SDK 0.13.3 owns product inference; pinned official llama.cpp owns raw/profiler evidence; both report the exact MedPsy SHA | `node --import tsx --test tests/structured-danger-contract.test.ts` | Runtime planes merge, identity drifts, or raw one-pass output is promoted as product safety | Build contract gate |
| FTR-004 | Supported pediatric respiratory intake | Structured age boundaries accept 2 through 59 months; seven descriptive fields expose only PRESENT/ABSENT/NOT_ASSESSED and omission never becomes absent | `node --import tsx --test tests/structured-danger-contract.test.ts` | Age comes from prose, CONFLICT is request-facing, or an omitted observation silently defaults absent | Build contract gate |
| FTR-005 | Strict model schema and fail-closed parsing | Malformed JSON, extra keys, `<think>`, oversized content, invented fields, and unknown source IDs never reach final UI | `node --import tsx --test tests/api.test.ts` | Any invalid model output produces a clinical result | Debug parser/adversarial suite |
| FTR-006 | Deterministic structured danger precedence | Six emergency-capable keys short-circuit before missing data/routing/model; incomplete state fails closed; chest indrawing alone is age-scoped non-emergency; all-absent invokes QVAC | `node --import tsx --test tests/structured-danger-contract.test.ts` | Chest indrawing auto-escalates, missing data reaches MedPsy, or a known emergency is suppressed | Build contract gate |
| FTR-007 | Reviewed local source binding | Every displayed action resolves to a signed, reviewed source record and full derived-fact hash | `npm run verify-sources && node --import tsx --test tests/api.test.ts` | Missing/unreviewed/stale source, invented citation, or unsigned action displays | Debug source integrity audit |
| FTR-008 | Offline localhost workflow and real stages | External negative control fails while one real browser-to-llama-to-policy-to-source result succeeds on localhost | `./scripts/verify-offline.sh` | Egress succeeds, any stage is synthetic, or local full flow fails | Wire and Livetest offline trace |
| FTR-009 | Cancellation, timeout, queue, and recovery | Concurrent submit gets 409; cancel/timeout releases slot; retry succeeds; stopped app and detached child remain stopped | `node --import tsx --test tests/service.test.ts tests/shutdown.test.ts tests/server.test.ts` | Waiting queue, leaked PID/port, automatic restart, or retry uses stale state | Debug lifecycle suite, Stress concurrency |
| FTR-010 | `medpsy-product-v2` / `medpsy-raw-profiler-v2` separation and parity | Product and profiler bind identical GGUF bytes while retaining distinct runtimes, schemas, claims, and host tiers | `node --import tsx --test tests/structured-danger-contract.test.ts` | Name-only parity, merged evidence planes, raw output presented as product safety, or CI promoted to target hardware | Build contract gate |
| FTR-011 | Early submission-survival checkpoint | Required root/report/prompts/profiler numeric fields/video plan/publication status are truthfully staged before optional work | `./scripts/early-checkpoint.sh` | Placeholder required field, fabricated URL/metric, prompt mismatch, or optional task before checkpoint | Package checklist, Verify preflight |
| FTR-012 | 118-second proof-led demo path | Final media is at most 120 seconds and visibly proves offline state, local inference, safety/source binding, same-hash evidence, and honest host labels | `ffprobe -v error -show_entries format=duration -of csv=p=0 demo/final.mp4 | awk '$1<=120{ok=1} END{exit !ok}'` | Over 120 seconds, old Triage-0 UI, hidden cloud path, fabricated metric, or missing proof scene | Demo rehearsal and Package |

## Release interpretation

- Observables are acceptance contracts, not claims that Build evidence already exists.
- FTR-002 cannot pass until fixed producers create and hash real finalist artifacts.
- FTR-009 cannot pass until an observation window proves restart suppression for both processes.
- FTR-010 and FTR-012 cannot use CI or Apple measurements as physical target-laptop evidence.
- Organizer approval remains unavailable until an external organizer-controlled trust key exists; therefore clean implementation is the only current provenance path.
